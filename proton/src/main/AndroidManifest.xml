<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application>
        <!-- The pause action of the thumbnail download notification; only this app holds its PendingIntent. -->
        <receiver
            android:name="com.bownee.lenswave.proton.ProtonThumbnailPauseReceiver"
            android:exported="false" />

        <!-- WorkManager promotes long-running thumbnail downloads to a data-sync service. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
